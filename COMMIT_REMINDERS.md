# ⏰ SignageERP - Commit Reminder System

## 🚨 AUTOMATIC REMINDERS

### ฉันจะแจ้งเตือนคุณเมื่อ:

1. ✅ **ทำงานไปนาน 30 นาที** โดยไม่ commit
2. ✅ **เพิ่มฟังก์ชันการคำนวณเงินใหม่** (pricing, discount, tax, etc.)
3. ✅ **เปลี่ยนโค้ดสำคัญ** มากกว่า 50 บรรทัด
4. ✅ **ก่อนเริ่มงานใหม่** (เช่น feature ใหม่)

---

## 📢 Reminder Format

เมื่อถึงเวลา ฉันจะพูดแบบนี้:

```
🔔 COMMIT REMINDER!

เวลาผ่านไป: 30 นาที
ไฟล์ที่แก้: 3 files
บรรทัดเปลี่ยน: ~80 lines

ควร commit ตอนนี้:
git add apps/api/src/pricing/
git commit -m "feat(pricing): add discount calculation"
git push

เหตุผล: มีการเปลี่ยนแปลงเกี่ยวกับการคำนวณเงินซึ่งเป็น critical logic
```

---

## 🎯 คุณควรทำ:

### เมื่อเห็น Reminder:

1. **Review Changes:**
   ```bash
   git status
   git diff
   ```

2. **Run Tests (ถ้ามี):**
   ```bash
   npm test
   ```

3. **Commit:**
   ```bash
   git add .
   git commit -m "feat(pricing): <what you did>"
   ```

4. **บอกฉัน:** "Committed" หรือ "Skip for now"

---

## 💰 Special Rules for Financial Code

### CRITICAL: ต้อง commit ทันทีเมื่อ:

- ✅ เพิ่ม/แก้ฟังก์ชันคำนวณราคา
- ✅ เปลี่ยน discount/tax logic
- ✅ แก้ rounding behavior
- ✅ เปลี่ยน pricing algorithm

**ห้าม skip reminder** เมื่อเกี่ยวกับเงิน!

---

## 📋 Reminder Checklist

เมื่อฉันเตือน ให้เช็ค:

- [ ] โค้ดรันได้? (`npm run dev`)
- [ ] Test pass? (`npm test`)
- [ ] ไฟล์ละเอียดอ่อน (.env) ไม่อยู่ใน commit?
- [ ] Commit message ชัดเจน?

---

## 🛑 คุณสามารถ:

### Skip Reminder:
```
คุณ: "Skip, still working on this function"
ฉัน: "OK, will remind again in 15 minutes"
```

### Force Reminder Off (ไม่แนะนำ):
```
คุณ: "Disable reminders for this session"
ฉัน: "⚠️ Reminders disabled. Remember to commit manually!"
```

---

## 🎓 Training Mode

สำหรับ 2 สัปดาห์แรก:

- ฉันจะเตือน **ทุก 20 นาที** (บ่อยกว่าปกติ)
- เพื่อสร้างนิสัยการ commit บ่อย
- หลัง 2 สัปดาห์ → ปรับเป็น 30 นาที

---

## 💡 Pro Tip

ตั้งนาฬิกาปลุกของคุณเองด้วย:
- Windows: `Alarms & Clock` app
- หรือใช้ Pomodoro timer (25 นาที)

---

**จำไว้:** การ commit บ่อย = ประหยัด tokens + เวลา + ความเครียด! 🚀
