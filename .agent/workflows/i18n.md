---
description: How to update i18n translations when adding/editing features
---

# 🌐 i18n Translation Workflow

> **ใช้ workflow นี้ทุกครั้งที่เพิ่ม/แก้ไข UI text** เพื่อให้แน่ใจว่าทุกภาษามีคำแปล

---

## ✅ Checklist สำหรับเพิ่มฟีเจอร์ใหม่

เมื่อคุณกำลังสร้างฟีเจอร์ใหม่ที่มีข้อความแสดงผล (UI text):

### 1. เขียนโค้ดด้วย `useTranslations()` เลย

```typescript
// ❌ อย่าเขียนแบบนี้
<h1>รายการวัสดุทั้งหมด</h1>

// ✅ เขียนแบบนี้ตั้งแต่แรก
import {useTranslations} from 'next-intl';

const t = useTranslations('materials');
<h1>{t('title')}</h1>
```

### 2. เพิ่มคำแปลใน `messages/th.json` (ภาษาหลัก)

```json
{
  "materials": {
    "title": "รายการวัสดุทั้งหมด",
    "newKey": "ข้อความใหม่ที่เพิ่ม"  // ← เพิ่มตรงนี้
  }
}
```

### 3. ⚠️ **อย่าลืม!** เพิ่มใน `messages/en.json` ด้วย

```json
{
  "materials": {
    "title": "All Materials",
    "newKey": "New text added"  // ← ต้องเพิ่มที่นี่ด้วย!
  }
}
```

### 4. ⚠️ **อย่าลืม!** เพิ่มใน `messages/my.json` ด้วย (สำหรับพนักงานพม่า)

```json
{
  "materials": {
    "title": "ပစ္စည်းအားလုံး",
    "newKey": "အသစ်ထားထားသောစာသား"  // ← ต้องเพิ่มที่นี่ด้วย!
  }
}
```

### 5. ตรวจสอบว่าครบทั้ง 3 ภาษา

```bash
# ดูว่าทั้ง 3 ไฟล์มี key เดียวกันหรือเปล่า
code apps/web/messages/th.json
code apps/web/messages/en.json
code apps/web/messages/my.json
```

---

## 🔄 Checklist สำหรับแก้ไขข้อความเดิม

เมื่อคุณต้องการเปลี่ยนข้อความ:

1. แก้ใน `messages/th.json`
2. แก้ใน `messages/en.json`
3. แก้ใน `messages/my.json`
4. โค้ดไม่ต้องแก้ (เพราะใช้ key เดิม)

---

## 📋 เทคนิคไม่ให้ลืม

### วิธีที่ 1: เช็คก่อน Commit

สร้าง Git pre-commit hook ตรวจสอบว่า keys ครบทุกภาษา:

```bash
# สร้างไฟล์ .git/hooks/pre-commit
#!/bin/bash

TH_KEYS=$(jq -r 'keys[]' apps/web/messages/th.json | sort)
EN_KEYS=$(jq -r 'keys[]' apps/web/messages/en.json | sort)
MY_KEYS=$(jq -r 'keys[]' apps/web/messages/my.json | sort)

if [ "$TH_KEYS" != "$EN_KEYS" ] || [ "$TH_KEYS" != "$MY_KEYS" ]; then
  echo "❌ Translation keys mismatch! Check th.json, en.json, my.json"
  exit 1
fi

echo "✅ All translation keys match"
```

### วิธีที่ 2: ใช้ VS Code Extension

ติดตั้ง Extension: **i18n Ally**

- แสดงคำแปลทั้งหมดใน sidebar
- เตือนเมื่อมี key ขาดหาย
- แก้ไขได้หลายภาษาพร้อมกัน

### วิธีที่ 3: เขียน TODO Comment

```typescript
// TODO: เพิ่มคำแปล en.json, my.json
const t = useTranslations('newModule');
```

---

## 🎯 แนวทางที่ดีที่สุด

1. **เขียน UI ใหม่ → เพิ่มคำแปลทันที** (อย่าเอาไว้ทีหลัง)
2. **ใช้ module ชัดเจน** (เช่น `materials`, `stock`, `kanban`)
3. **ตั้งชื่อ key ให้สื่อความหมาย** (เช่น `addButton`, `deleteConfirm`)
4. **อย่า hard-code ข้อความ** (ใช้ `t('key')` เสมอ)

---

## 📂 โครงสร้างไฟล์

```
apps/web/messages/
├── th.json  ← ภาษาไทย (หลัก)
├── en.json  ← ภาษาอังกฤษ (สำรอง)
└── my.json  ← ภาษาพม่า (สำหรับพนักงาน)
```

**กฎทอง:** ทุก key ที่มีใน `th.json` ต้องมีใน `en.json` และ `my.json` เหมือนกัน!

---

## ⚡ Quick Reference

| สถานการณ์ | ทำอย่างไร |
|----------|----------|
| เพิ่มข้อความใหม่ | เพิ่มใน th.json → en.json → my.json |
| แก้ไขข้อความ | แก้ใน th.json → en.json → my.json |
| ลบข้อความ | ลบจาก th.json → en.json → my.json |
| เปลี่ยนโครงสร้าง | Refactor ทั้ง 3 ไฟล์พร้อมกัน |

---

**สรุป:** ทุกครั้งที่แก้ `th.json` ต้องแก้ `en.json` และ `my.json` ด้วย เสมอ! 🎯
